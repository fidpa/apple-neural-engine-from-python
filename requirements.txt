# Pinned to the versions that worked on M4 Pro / macOS / Python 3.12.7.
# coremltools 8.3.0 is REQUIRED — 7.x fails to load CoreML.framework on
# Python 3.12 (error: "Unable to load CoreML.framework").
# transformers is needed by ane_working.py (BertTokenizer).
coremltools==8.3.0
transformers==4.36.2
numpy==1.26.3
torch==2.5.1
